#include <stdio.h>
#include<math.h>
int main()
{
   int num,sum=0;
   printf("Enter a number");
   scanf("%d",&num);
  if(num>0)
  {
      sum=sqrt(num);
  }
   printf("the square of number=%d",sum);
}

